name: 🤖 MFLIX 24/7 Auto-Pilot

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  t0:
    name: 'Queue Run (0m)'
    runs-on: ubuntu-latest
    timeout-minutes: 4
    steps:
      - name: Run
        run: |
          curl -sf -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            "${{ secrets.APP_BASE_URL }}/api/cron/process-queue" || true

  t1:
    name: 'Queue Run (1m)'
    runs-on: ubuntu-latest
    timeout-minutes: 4
    steps:
      - run: sleep 60
      - name: Run
        run: |
          curl -sf -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            "${{ secrets.APP_BASE_URL }}/api/cron/process-queue" || true

  t2:
    name: 'Queue Run (2m)'
    runs-on: ubuntu-latest
    timeout-minutes: 4
    steps:
      - run: sleep 120
      - name: Run
        run: |
          curl -sf -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            "${{ secrets.APP_BASE_URL }}/api/cron/process-queue" || true

  t3:
    name: 'Queue Run (3m)'
    runs-on: ubuntu-latest
    timeout-minutes: 4
    steps:
      - run: sleep 180
      - name: Run
        run: |
          curl -sf -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            "${{ secrets.APP_BASE_URL }}/api/cron/process-queue" || true

  t4:
    name: 'Queue Run (4m)'
    runs-on: ubuntu-latest
    timeout-minutes: 4
    steps:
      - run: sleep 240
      - name: Run
        run: |
          curl -sf -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            "${{ secrets.APP_BASE_URL }}/api/cron/process-queue" || true
